********
Integers
********

We can have functions with name starting with ``Example`` in our tests, which
document the usage of our code. They'll fail to build if the output in comments
doesn't match the actual output.

To run then we do ``go test -v``.

They also show up in the godoc for our package.

